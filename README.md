Testing automatic positioning of elements by using a grid, which is responsive to innerWidth.
I divide innerWidth by whatever amount of elements i want per line.

Coding with avoidance of hard coding in mind from the beginning.

Having fun with arrays, reading index of class constructor instances to position each instance in a row, graphically.
Figured out successfully how to code class constructor in such a way that each instance can recieve differing x values, where i 
identify each element by their position in the array, and set up a logic so that i can multiply the
array index by doing the following: 
innerwidth / elements_per_row * array_index

If instances are greater than set amount of elements per line, put next element in array to next line and continue pattern.

Testing and trying to flesh out a project file setup style, for better organization, readability and better overview over events.

Going more in depth with console.log() to get better response and overview on live events.
This by gradually creating debug or info tools parallel with making the project.
